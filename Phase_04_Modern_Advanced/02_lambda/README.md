# 02 Lambda

Create inline anonymous functions with lambda expressions and captures.

## What to learn

- Create inline anonymous functions with lambda expressions and captures.
- Example code in `source/main.cpp`
- Exercise prompt in `exercise/exercise.cpp`

## Compile

```bash
g++ -std=c++17 source/main.cpp -o main
./main
```
