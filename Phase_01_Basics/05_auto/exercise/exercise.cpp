#include <iostream>
#include <map>
#include <string>

// Exercise: use auto to iterate over a map of string keys to int values.

int main() {
    std::cout << "Exercise: use auto in a loop over a map.\n";
    return 0;
}
