# 02 Smart Pointers

Use unique_ptr, shared_ptr, and weak_ptr for safer memory ownership.

## What to learn

- Use unique_ptr, shared_ptr, and weak_ptr for safer memory ownership.
- Example code in `source/main.cpp`
- Exercise prompt in `exercise/exercise.cpp`

## Compile

```bash
g++ -std=c++17 source/main.cpp -o main
./main
```
