# 01 Class Struct

Model data with classes and structs to encapsulate behavior and state.

## What to learn

- Model data with classes and structs to encapsulate behavior and state.
- Example code in `source/main.cpp`
- Exercise prompt in `exercise/exercise.cpp`

## Compile

```bash
g++ -std=c++17 source/main.cpp -o main
./main
```
